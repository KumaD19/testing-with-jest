import { HeaderStyled, HeaderTitle } from "./styles";

const Header = () => {
  return (
    <HeaderStyled>
        <HeaderTitle>STORE</HeaderTitle>
    </HeaderStyled>
  );
};

export default Header;
